#
# Change the profile used by the audio card.
# Relevant only for satsuki.
#

id=$(pw-cli ls Device | grep -B 6 alsa_card.pci-0000_00_1f.3-platform-skl_hda_dsp_generic | head -n 1 | cut -d ' ' -f 2 | sed 's/,//')

speaker_index=$(pw-cli e "$id" EnumProfile | grep -B 2 'HiFi (HDMI1, HDMI2, HDMI3, Mic1, Mic2, Speaker)' | head -n 1 | xargs | cut -d ' ' -f 2)

pw-cli s "$id" Profile "{ index: $speaker_index, save: true }"
