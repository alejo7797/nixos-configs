#! /usr/bin/env nix-shell
#! nix-shell -i bash -p bash gnugrep
# shellcheck shell=bash

if grep -q open /proc/acpi/button/lid/LID0/state; then
    swaymsg output eDP-1 enable
else
    swaymsg output eDP-1 disable
fi

# vim:ft=sh
